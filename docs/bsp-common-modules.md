# Общие модули БСП — справочник

Краткий справочник по публичному API общих модулей БСП, которые чаще всего используются в проекте. Цель — не заставлять Claude (и человека) писать вручную то, что уже есть в БСП. Полные описания методов — в исходниках БСП в хост-проекте базовой конфигурации (каталог — см. [`../CLAUDE.md`](../CLAUDE.md) → «EDT-воркспейс»):

- `<хост-проект>/src/CommonModules/<ИмяМодуля>/Module.bsl`

В справочник включены **только публичные методы** (`#Область ПрограммныйИнтерфейс`). Устаревшие (`УстаревшиеПроцедурыИФункции`) и служебные (`СлужебныйПрограммныйИнтерфейс`, `СлужебныеПроцедурыИФункции`) намеренно исключены — расширению пользоваться ими нельзя.

> **Не путать с проектными модулями `<prefix>_*`.** В проекте могут быть собственные общие модули с похожими именами — например, `<prefix>_ОбщегоНазначения`, `<prefix>_ОбщегоНазначенияКлиентСервер`. Это **другие** модули с проектными хелперами. Когда видите вызов вида `ОбщегоНазначения*.Метод(...)` без префикса проекта — это БСП из этого справочника. С префиксом проекта — проектный модуль, описание ищется в исходниках расширения, не здесь. Аналогично для `Пользователи` vs `<prefix>_Пользователи*` и т.п.

## Какой модуль брать в каком контексте

| Контекст вызывающего кода | Подходящий суффикс модуля |
| --- | --- |
| Серверная процедура / `&НаСервере` / `&НаСервереБезКонтекста` | `ОбщегоНазначения`, `СтроковыеФункции`, `Пользователи`, `ФайловаяСистема` |
| Клиентская процедура (`&НаКлиенте`, без серверного контекста) | `ОбщегоНазначенияКлиент`, `СтроковыеФункцииКлиент`, `ПользователиКлиент`, `ФайловаяСистемаКлиент` |
| Клиент-серверный модуль или процедура без `&НаКлиенте/&НаСервере` | `ОбщегоНазначенияКлиентСервер`, `СтроковыеФункцииКлиентСервер`, `ПользователиКлиентСервер` |
| Прямой серверный вызов из клиентского контекста (без оборачивания в собственный серверный модуль) | `ОбщегоНазначенияВызовСервера` |
| Локализованные функции для русского языка | `СтроковыеФункцииКлиентСерверРФ` |

> **Правило выбора:** если метод нужен и на клиенте, и на сервере — берём `*КлиентСервер`. Если только на одной стороне — соответствующий модуль. Не дублировать клиент-серверную логику в двух местах.

---

## ОбщегоНазначения / *Клиент / *КлиентСервер / *ВызовСервера

### Сообщения пользователю

| Метод | Модуль |
| --- | --- |
| `СообщитьПользователю(...)` | `ОбщегоНазначения`, `ОбщегоНазначенияКлиент` |
| `ДобавитьОшибкуПользователю(...)` | `ОбщегоНазначенияКлиентСервер` |
| `СообщитьОшибкиПользователю(Ошибки, Отказ = Ложь)` | `ОбщегоНазначенияКлиентСервер` |
| `ТекстОшибкиЗаполнения(...)` | `ОбщегоНазначенияКлиентСервер` |
| `ПутьКТабличнойЧасти(...)` | `ОбщегоНазначенияКлиентСервер` |

Платформенный `Сообщить()` использовать **запрещено** — только `СообщитьПользователю`.

### Чтение реквизитов объектов

| Метод | Назначение |
| --- | --- |
| `ЗначениеРеквизитаОбъекта(Ссылка, ИмяРеквизита, ВыбратьРазрешенные = Ложь, КодЯзыка = Неопределено)` | Один реквизит одного объекта |
| `ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты, ...)` | Несколько реквизитов одного объекта (`Реквизиты` = строка через запятую или структура) |
| `ЗначениеРеквизитаОбъектов(МассивСсылок, ИмяРеквизита, ...)` | Один реквизит для многих объектов |
| `ЗначенияРеквизитовОбъектов(Ссылки, Реквизиты, ...)` | Несколько реквизитов для многих объектов |

Все — только в серверном `ОбщегоНазначения`.

> **Антипаттерн:** `Ссылка.Реквизит` — критический. Любое обращение к реквизиту через точку — через эти методы.

### Запись реквизитов

- `УстановитьЗначениеРеквизита(Объект, ИмяРеквизита, Значение, КодЯзыка = Неопределено)`
- `УстановитьЗначенияРеквизитов(Объект, Значения, КодЯзыка = Неопределено)`

### Предопределённые элементы

- `ОбщегоНазначения.ПредопределенныйЭлемент(ПолноеИмяПредопределенного)` — серверный
- `ОбщегоНазначенияКлиент.ПредопределенныйЭлемент(...)` — клиентский (кэшируется)

Брать вместо `ПредопределенноеЗначение("...")` — обрабатывает удалённые предопределённые без ошибки.

### Ссылочные операции

- `ЕстьСсылкиНаОбъект(СсылкаИлиМассивСсылок, ИскатьСредиСлужебныхОбъектов = Ложь)`
- `ЗаменитьСсылки(ПарыЗамен, ПараметрыЗамены = Неопределено)` — массовая замена ссылок
- `ПараметрыЗаменыСсылок()` — структура параметров
- `МестаИспользования(НаборСсылок, АдресРезультата = "", ДополнительныеПараметры = Неопределено)`
- `ПараметрыПоискаМестИспользования()`
- `ИсключенияПоискаСсылок()`
- `ПодчиненныеОбъекты()`

### Метаданные

**Проверка типа объекта** (`ОбщегоНазначения.Это*(ОбъектМетаданных)`):

`ЭтоДокумент`, `ЭтоСправочник`, `ЭтоПеречисление`, `ЭтоПланОбмена`, `ЭтоПланВидовХарактеристик`, `ЭтоБизнесПроцесс`, `ЭтоЗадача`, `ЭтоПланСчетов`, `ЭтоПланВидовРасчета`, `ЭтоРегистрСведений`, `ЭтоРегистрНакопления`, `ЭтоРегистрБухгалтерии`, `ЭтоРегистрРасчета`, `ЭтоКонстанта`, `ЭтоЖурналДокументов`, `ЭтоПоследовательность`, `ЭтоРегламентноеЗадание`, `ЭтоРегистр`, `ЭтоОбъектСсылочногоТипа`.

**Работа со ссылками и таблицами:**

- `ИмяТаблицыПоСсылке(Ссылка)`
- `ВидОбъектаПоСсылке(Ссылка)` / `ВидОбъектаПоТипу(ТипОбъекта)`
- `СсылкаСуществует(ПроверяемаяСсылка)`
- `ЗначениеСсылочногоТипа(Значение)`
- `ЭтоСсылка(ПроверяемыйТип)`
- `ОбъектЯвляетсяГруппой(Объект)`
- `ИменаРеквизитовПоТипу(Ссылка, Тип)`
- `ИмяБазовогоТипаПоОбъектуМетаданных(ОбъектМетаданных)`

**Менеджеры объектов:**

- `МенеджерОбъектаПоПолномуИмени(ПолноеИмя)` — например, `"Справочник.Контрагенты"`
- `МенеджерОбъектаПоСсылке(Ссылка)`

**Идентификаторы объектов метаданных:**

- `ИдентификаторОбъектаМетаданных(ОписаниеОбъектаМетаданных, ВызыватьИсключение = Истина)`
- `ИдентификаторыОбъектовМетаданных(ОписаниеОбъектовМетаданных, ...)`
- `ОбъектМетаданныхПоИдентификатору(Идентификатор, ...)`
- `ОбъектыМетаданныхПоИдентификаторам(...)`
- `ОбъектМетаданныхДоступенПоФункциональнымОпциям(ОбъектМетаданных)`

**Описания типов:**

- `ОписаниеТипаСтрока(ДлинаСтроки)`
- `ОписаниеТипаЧисло(Разрядность, РазрядностьДробнойЧасти = 0, ЗнакЧисла = Неопределено)`
- `ОписаниеТипаДата(ЧастиДаты)`
- `ОписаниеТипаВсеСсылки()`
- `ОписаниеТипаСостоитИзТипа(ОписаниеТипа, ТипЗначения)`

**Прочее:**

- `ЭтоСтандартныйРеквизит(СтандартныеРеквизиты, ИмяРеквизита)`
- `ЕстьРеквизитОбъекта(ИмяРеквизита, МетаданныеОбъекта)`
- `ОписаниеСвойствОбъекта(ОбъектМетаданных, Свойства)`
- `СтроковоеПредставлениеТипа(Тип)`
- `ПредставлениеСписка(ОбъектМетаданных)` / `ПредставлениеОбъекта(ОбъектМетаданных)`
- `ДобавитьПереименование(Итог, ВерсияИБ, СтароеПолноеИмя, НовоеПолноеИмя, ИдентификаторБиблиотеки = "")`

### Текущее окружение

Серверные и клиентские (большинство дублируется):

- `ЭтоWindowsКлиент()`, `ЭтоWindowsСервер()`, `ЭтоLinuxКлиент()`, `ЭтоLinuxСервер()`, `ЭтоMacOSКлиент()`
- `ЭтоВебКлиент()`, `ЭтоМобильныйКлиент()`
- `КлиентПодключенЧерезВебСервер()`
- `РежимОтладки()`
- `ОперативнаяПамятьДоступнаяКлиентскомуПриложению()`
- `ИнформационнаяБазаФайловая(СтрокаСоединенияИнформационнойБазы = "")`
- `ЭтоАвтономноеРабочееМесто()`
- `ЭтоПодчиненныйУзелРИБ()`, `ЭтоПодчиненныйУзелРИБСФильтром()`, `ТребуетсяОбновлениеКонфигурацииПодчиненногоУзлаРИБ()`
- `РазделениеВключено()`, `ДоступноИспользованиеРазделенныхДанных()`
- `АдресПубликацииИнформационнойБазыВИнтернете()`, `АдресПубликацииИнформационнойБазыВЛокальнойСети()`
- `АдресВходаВПрограмму(Пользователь, Пароль, ВидПубликацииИБ)`
- `РедакцияКонфигурации()`
- `ОбщиеПараметрыБазовойФункциональности()`
- `ОписанияПодсистем()`
- `ИдентификаторИнтернетПоддержкиКонфигурации()`

Клиентский `ОбщегоНазначенияКлиент` дополнительно: `ТипПлатформыКлиента()`.

`ОбщегоНазначенияКлиентСервер.КаталогФайловойИнформационнойБазы()` — если нужно из общего модуля.

### Подсистемы и общие модули (условные вызовы)

- `ПодсистемаСуществует(ПолноеИмяПодсистемы)` — серверный и клиентский
- `ОбщийМодуль(Имя)` — серверный и клиентский, возвращает менеджер модуля по имени-строке. Используется при вызовах подсистем БСП, наличие которых не гарантировано.

### Даты

**Серверные (`ОбщегоНазначения`):**

- `ПредставлениеЛокальнойДатыСоСмещением(ЛокальнаяДата)`
- `ИнтервалВремениСтрокой(ВремяНачала, ВремяОкончания = Неопределено)`
- `УстановитьРабочуюДатуПользователя(НоваяРабочаяДата, ИмяПользователя = Неопределено)`
- `РабочаяДатаПользователя(ИмяПользователя = Неопределено)`
- `ТекущаяДатаПользователя(ИмяПользователя = Неопределено)`

**Клиентские (`ОбщегоНазначенияКлиент`):**

- `ДатаСеанса()`, `ДатаУниверсальная()` — для получения серверного времени с клиента
- `ПредставлениеЛокальнойДатыСоСмещением(...)`

> Платформенный `ТекущаяДатаСеанса()` — это **не** БСП, это платформа. На сервере брать его, на клиенте — `ОбщегоНазначенияКлиент.ДатаСеанса()`. `ТекущаяДата()` использовать запрещено.

### Коллекции (массивы, структуры, таблицы)

**Серверный `ОбщегоНазначения`:**

- `ТаблицаЗначенийВМассив(ТаблицаЗначений)`
- `СтрокаТаблицыЗначенийВСтруктуру(СтрокаТаблицыЗначений)`
- `СтруктураПоМенеджеруЗаписи(МенеджерЗаписи, МетаданныеРегистра)`
- `ВыгрузитьКолонку(КоллекцияСтрок, ИмяКолонки, ТолькоУникальныеЗначения = Ложь)`
- `ПрочитатьXMLВТаблицу(XML)`
- `КоллекцииИдентичны(КоллекцияСтрок1, КоллекцияСтрок2, ИменаКолонок = "", ИсключаяКолонки = "", ...)`
- `ДанныеСовпадают(Данные1, Данные2)`
- `ФиксированныеДанные(Данные, ВызыватьИсключение = Истина)`
- `СкопироватьРекурсивно(Источник, ФиксироватьДанные = Неопределено)` — есть и в `ОбщегоНазначенияКлиент`
- `УдалитьНепроверяемыеРеквизитыИзМассива(МассивРеквизитов, МассивНепроверяемыхРеквизитов)`
- `ИмяЗначенияПеречисления(Значение)`
- `КонтрольнаяСуммаСтрокой(Данные, Алгоритм = Неопределено)`
- `СократитьСтрокуКонтрольнойСуммой(Строка, МаксимальнаяДлина)`
- `ПредметСтрокой(СсылкаНаПредмет)`

**Клиент-серверный `ОбщегоНазначенияКлиентСервер`:**

- `ДополнитьТаблицу(ТаблицаИсточник, ТаблицаПриемник)`
- `ДополнитьТаблицуИзМассива(Таблица, Массив, ИмяПоля)`
- `ДополнитьМассив(МассивПриемник, МассивИсточник, ТолькоУникальныеЗначения = Ложь)`
- `ДополнитьСтруктуру(Приемник, Источник, Заменять = Неопределено)`
- `ДополнитьСоответствие(Приемник, Источник, Заменять = Неопределено)`
- `УдалитьВсеВхожденияЗначенияИзМассива(Массив, Значение)`
- `УдалитьВсеВхожденияТипаИзМассива(Массив, Тип)`
- `УдалитьЗначениеИзМассива(Массив, Значение)`
- `СвернутьМассив(Массив)` — оставить только уникальные значения
- `РазностьМассивов(Массив, МассивВычитания)`
- `СпискиЗначенийИдентичны(Список1, Список2)`
- `ЗначениеВМассиве(Значение)` — обернуть одно значение в массив
- `КлючиСтруктурыВСтроку(Структура, Разделитель = ",")`
- `СвойствоСтруктуры(Структура, Ключ, ЗначениеПоУмолчанию = Неопределено)` — **предпочитать** платформенному `Структура.Свойство(Имя, Знач)` (см. memory)
- `ПустойУникальныйИдентификатор()`
- `ЕстьРеквизитИлиСвойствоОбъекта(Объект, ИмяРеквизита)`

### Проверки и контракты

- `ОбщегоНазначенияКлиентСервер.Проверить(Условие, Сообщение = "", КонтекстПроверки = "")` — assert
- `ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(ИмяПроцедурыИлиФункции, ИмяПараметра, ЗначениеПараметра, ...)` — проверка типа параметра

### Формы

**Клиент-серверные (`ОбщегоНазначенияКлиентСервер`):**

- `ПолучитьРеквизитФормыПоПути(Форма, ПутьРеквизита)`
- `УстановитьРеквизитФормыПоПути(Форма, ПутьРеквизита, Значение, ТолькоЕслиНеЗаполнен = Ложь)`
- `НайтиЭлементОтбораПоПредставлению(КоллекцияЭлементов, Представление)`
- `УстановитьСвойствоЭлементаФормы(ЭлементыФормы, ИмяЭлемента, ИмяСвойства, Значение)`
- `ЗначениеСвойстваЭлементаФормы(ЭлементыФормы, ИмяЭлемента, ИмяСвойства)`
- `КартинкаКомментария(Комментарий)`

**Клиентские (`ОбщегоНазначенияКлиент`):**

- `ПоказатьПодтверждениеЗакрытияФормы(...)` / `ПоказатьПодтверждениеЗакрытияПроизвольнойФормы(...)`
- `ОбновитьИнтерфейсПрограммы()`
- `ОповеститьОбИзмененииОбъекта(Источник, ДополнительныеПараметры = Неопределено)`
- `ОповеститьОбИзмененииОбъектов(Источник, ДополнительныеПараметры = Неопределено)`
- `ПоказатьВыборФорматаВложений(ОписаниеОповещения, НастройкиФормата, Владелец = Неопределено)`
- `ПоказатьФормуРедактированияМногострочногоТекста(...)`
- `ПоказатьФормуРедактированияКомментария(...)`

### Динамический список

**Серверный `ОбщегоНазначения`:**

- `СтруктураСвойствДинамическогоСписка()`
- `УстановитьСвойстваДинамическогоСписка(Список, СтруктураПараметров)`

**Клиент-серверный (`ОбщегоНазначенияКлиентСервер`):**

- `НайтиЭлементыИГруппыОтбора(ОбластьПоиска, ...)`
- `СоздатьГруппуЭлементовОтбора(КоллекцияЭлементов, Представление, ТипГруппы)`
- `ДобавитьЭлементКомпоновки(ОбластьДобавления, ...)`
- `ИзменитьЭлементыОтбора(ОбластьПоиска, ...)`
- `УдалитьЭлементыГруппыОтбора(ОбластьУдаления, ИмяПоля = Неопределено, Представление = Неопределено)`
- `УстановитьЭлементОтбора(ОбластьПоискаДобавления, ...)`
- `УстановитьЭлементОтбораДинамическогоСписка(ДинамическийСписок, ИмяПоля, ...)`
- `УдалитьЭлементыГруппыОтбораДинамическогоСписка(ДинамическийСписок, ...)`
- `УстановитьПараметрДинамическогоСписка(Список, ИмяПараметра, Значение, Использование = Истина)`

### Версионирование конфигураций

- `ВерсияКонфигурацииБезНомераСборки(Версия)`
- `СравнитьВерсии(СтрокаВерсии1, СтрокаВерсии2)`
- `СравнитьВерсииБезНомераСборки(СтрокаВерсии1, СтрокаВерсии2)`

### Имена файлов и пути (без обращений к диску)

`ОбщегоНазначенияКлиентСервер` — все только парсинг строк-путей:

- `ДобавитьКонечныйРазделительПути(ПутьКаталога, Платформа = Неопределено)`
- `ПолучитьПолноеИмяФайла(ИмяКаталога, ИмяФайла)`
- `РазложитьПолноеИмяФайла(ПолноеИмяФайла, ЭтоКаталог = Ложь)`
- `РазложитьСтрокуПоТочкамИСлэшам(Строка)`
- `ПолучитьРасширениеИмениФайла(ИмяФайла)`
- `РасширениеБезТочки(Расширение)`
- `ПолучитьИмяСРасширением(ИмяБезРасширения, Расширение)`
- `ПолучитьНедопустимыеСимволыВИмениФайла()`
- `НайтиНедопустимыеСимволыВИмениФайла(ИмяФайла)`
- `ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяФайла, НаЧтоМенять = " ")`

### Email-адреса

`ОбщегоНазначенияКлиентСервер`:

- `АдресаЭлектроннойПочтыИзСтроки(СписокАдресов)`
- `АдресЭлектроннойПочтыСоответствуетТребованиям(Адрес, РазрешитьЛокальныеАдреса = Ложь)`
- `РазобратьСтрокуСПочтовымиАдресами(СтрокаПереданныхАдресов, ВызыватьИсключение = Истина)`

### XML / XDTO

**Серверные (`ОбщегоНазначения`):**

- `ЗначениеВСтрокуXML(Значение)` / `ЗначениеИзСтрокиXML(СтрокаXML)`
- `ОбъектXDTOВСтрокуXML(ОбъектXDTO, Фабрика = Неопределено)` / `ОбъектXDTOИзСтрокиXML(СтрокаXML, Фабрика = Неопределено)`

**Клиент-серверные (`ОбщегоНазначенияКлиентСервер`):**

- `ЗаменитьНедопустимыеСимволыXML(Текст, СимволЗамены = " ")`
- `УдалитьНедопустимыеСимволыXML(Текст)`

### Хранилища настроек

Серверные `ОбщегоНазначения` и `ОбщегоНазначенияВызовСервера` (зеркальный набор):

| Хранилище | Сохранить | Загрузить | Удалить |
| --- | --- | --- | --- |
| Общие настройки | `ХранилищеОбщихНастроекСохранить` | `ХранилищеОбщихНастроекЗагрузить` | `ХранилищеОбщихНастроекУдалить` |
| Системные настройки | `ХранилищеСистемныхНастроекСохранить` | `ХранилищеСистемныхНастроекЗагрузить` | `ХранилищеСистемныхНастроекУдалить` |
| Настройки данных форм | `ХранилищеНастроекДанныхФормСохранить` | `ХранилищеНастроекДанныхФормЗагрузить` | `ХранилищеНастроекДанныхФормУдалить` |

`ОбщегоНазначения` дополнительно: `ХранилищеОбщихНастроекСохранитьМассив(НесколькоНастроек, ...)`.

> **Не использовать** платформенные `ХранилищеОбщихНастроек.Сохранить/Загрузить` напрямую — они не работают в модели сервиса (разделение данных).

### Безопасное хранилище

Серверный `ОбщегоНазначения`:

- `ЗаписатьДанныеВБезопасноеХранилище(Владелец, Данные, Ключ = "Пароль")`
- `ПрочитатьДанныеИзБезопасногоХранилища(Владелец, Ключи = "Пароль", ОбщиеДанные = Неопределено)`
- `УдалитьДанныеИзБезопасногоХранилища(Владелец, Ключи = Неопределено)`

### Буфер обмена

Серверный `ОбщегоНазначения`:

- `СкопироватьСтрокиВБуферОбмена(ТабличнаяЧасть, ВыделенныеСтроки, Источник = Неопределено)`
- `СкопироватьВБуферОбмена(Данные, Источник = Неопределено)`
- `СтрокиИзБуфераОбмена()`
- `ПустойБуферОбмена(Источник = Неопределено)`

### Безопасное выполнение внешнего кода

Серверный `ОбщегоНазначения`:

- `ВыполнитьМетодКонфигурации(ИмяМетода, Параметры = Неопределено)`
- `ВыполнитьМетодОбъекта(Объект, ИмяМетода, Параметры = Неопределено)`
- `ВыполнитьВБезопасномРежиме(Алгоритм, Параметры = Неопределено)`
- `ВычислитьВБезопасномРежиме(Выражение, Параметры = Неопределено)`
- `ОписаниеЗащитыБезПредупреждений()`

Брать вместо платформенного `Выполнить`/`Вычислить` всегда, когда исполняемый код приходит извне.

### Веб-сервисы

Серверный `ОбщегоНазначения`:

- `СоздатьWSПрокси(ПереданныеПараметры)`
- `ПараметрыПодключенияWSПрокси()`
- `ПолучитьВерсииИнтерфейса(Адрес, Пользователь, Пароль = Неопределено, Интерфейс = Неопределено)`
- `ПолучитьВерсииИнтерфейсаЧерезВнешнееСоединение(ВнешнееСоединение, ИмяИнтерфейса)`
- `УдалитьЗаписиКэшаВерсий(ПодстрокаПоискаИдентификаторов)`

### Внешнее соединение (COM)

- `ОбщегоНазначения.ИдентификаторCOMСоединителя(ИмяCOMСоединителя)`
- `ОбщегоНазначения.УстановитьВнешнееСоединениеСБазой(Параметры)` — есть и в `ОбщегоНазначенияКлиент`
- `ОбщегоНазначенияКлиент.ЗарегистрироватьCOMСоединитель(...)`
- `ОбщегоНазначенияКлиентСервер.ИмяCOMСоединителя()`
- `ОбщегоНазначенияКлиентСервер.СтруктураПараметровДляУстановкиВнешнегоСоединения()`
- `ОбщегоНазначенияКлиентСервер.ПолучитьПараметрыПодключенияИзСтрокиСоединенияИнформационнойБазы(СтрокаСоединения)`

### Внешние компоненты

- `ОбщегоНазначения.ПодключитьКомпонентуИзМакета(Идентификатор, ПолноеИмяМакета)` — синхронный, серверный
- `ОбщегоНазначенияКлиент.ПараметрыПодключенияКомпоненты()`
- `ОбщегоНазначенияКлиент.ПодключитьКомпонентуИзМакета(Оповещение, Идентификатор, ПолноеИмяМакета, ...)` — асинхронный
- `ОбщегоНазначенияКлиент.ПараметрыУстановкиКомпоненты()`
- `ОбщегоНазначенияКлиент.УстановитьКомпонентуИзМакета(Оповещение, ПолноеИмяМакета, ПараметрыУстановки = Неопределено)`

### Запросы (помощники)

Серверный `ОбщегоНазначения`:

- `СформироватьСтрокуДляПоискаВЗапросе(СтрокаПоиска)` — экранирование спецсимволов для `ПОДОБНО`
- `РазделительПакетаЗапросов()`

### Документы

Серверные `ОбщегоНазначения` и `ОбщегоНазначенияВызовСервера`:

- `ПроверитьПроведенностьДокументов(Документы)`
- `ПровестиДокументы(Документы)`

### Языки

Серверный `ОбщегоНазначения`:

- `КодОсновногоЯзыка()` — есть и в `ОбщегоНазначенияКлиент`
- `ЭтоОсновнойЯзык()`

### Математика

`ОбщегоНазначенияКлиентСервер.РаспределитьСуммуПропорциональноКоэффициентам(РаспределяемаяСумма, Коэффициенты, Точность = 2)` — есть и в `ОбщегоНазначения` (старая сигнатура), и в `ОбщегоНазначенияКлиент`.

### Стили (Клиент)

- `ОбщегоНазначенияКлиент.ЦветСтиля(ИмяЦветаСтиля)`
- `ОбщегоНазначенияКлиент.ШрифтСтиля(ИмяШрифтаСтиля)`

Брать вместо ручного `WebЦвета.КрасныйЯркий` и т.п. — позволяет переопределять оформление централизованно.

### Резервное копирование (Клиент)

- `ОбщегоНазначенияКлиент.ПредлагатьСоздаватьРезервныеКопии()`
- `ОбщегоНазначенияКлиент.ПредложитьПользователюСоздатьРезервнуюКопию()`

### Интернет (КлиентСервер)

- `СтруктураURI(СтрокаURI)`
- `НовоеЗащищенноеСоединение(СертификатКлиента = Неопределено, СертификатыУдостоверяющихЦентров = Неопределено)`

### Параметры устройства (КлиентСервер)

- `ТипУстройства()`
- `ПараметрыЭкранаУстройства()`

### Табличный документ (КлиентСервер)

- `УстановитьСостояниеПоляТабличногоДокумента(ПолеТабличногоДокумента, Состояние = "НеИспользовать")`
- `РасчетныеПоказателиЯчеек(ТабличныйДокумент, ПолеТабличногоДокумента)`

### Регламентные задания (КлиентСервер)

- `РасписаниеВСтруктуру(Расписание)` / `СтруктураВРасписание(СтруктураРасписания)`
- `РасписанияОдинаковые(Расписание1, Расписание2)`

### Прочее

- `ОбщегоНазначения.ПриНачалеВыполненияРегламентногоЗадания(РегламентноеЗадание = Неопределено)`
- `ОбщегоНазначения.ОчиститьПараметрыСеанса(ПараметрыДляОчистки = "", Исключения = "")`
- `ОбщегоНазначения.ПроверитьВыводТабличногоДокумента(ТабДокумент, ВыводимыеОбласти, РезультатПриОшибке = Истина)`
- `ОбщегоНазначения.СохранитьПерсональныеНастройки(Настройки)` (есть и в `ОбщегоНазначенияКлиент`)
- `ОбщегоНазначения.ЗаполнитьКоллекциюЭлементовДереваДанныхФормы(КоллекцияЭлементовДерева, ДеревоЗначений)`
- `ОбщегоНазначенияКлиент.ПроверитьТипПараметраКоманды(Параметр, ОжидаемыйТип)`
- `ОбщегоНазначенияКлиентСервер.ОтмеченныеЭлементы(Список)` — для `СписокЗначений` с пометками
- `ОбщегоНазначенияКлиентСервер.СнятьУсловноеОформлениеСпискаЗначений(...)`
- `ОбщегоНазначенияКлиентСервер.ПолучитьИдентификаторСтрокиДереваПоЗначениюПоля(...)`

---

## СтроковыеФункции / *Клиент / *КлиентСервер / *КлиентСерверРФ

### СтроковыеФункцииКлиентСервер — основной инструментарий

**Разбор и сборка:**

- `РазложитьСтрокуВМассивПодстрок(Значение, Разделитель = ",", ПропускатьПустыеСтроки = Неопределено, ...)`
- `ЭтоРазделительСлов(КодСимвола, РазделителиСлов = Неопределено)`
- `РазложитьСтрокуВМассивСлов(Значение, РазделителиСлов = Неопределено)`
- `ПодставитьПараметрыВСтроку(ШаблонСтроки, ...)` — позиционные `%1`, `%2`, ...
- `ПодставитьПараметрыВСтрокуИзМассива(ШаблонСтроки, Параметры)`
- `ВставитьПараметрыВСтроку(ШаблонСтроки, Параметры)` — именованные `[Имя]`
- `ПараметрыИзСтроки(СтрокаПараметров, Разделитель = ";")`

**Проверки:**

- `ТолькоЦифрыВСтроке(Значение, Устаревший = Истина, ПробелыЗапрещены = Истина)`
- `ТолькоЛатиницаВСтроке(СтрокаПроверки, УчитыватьРазделителиСлов = Истина, ДопустимыеСимволы = "")`
- `ЭтоУникальныйИдентификатор(Значение)`
- `СтроковыеФункцииКлиентСерверРФ.ТолькоКириллицаВСтроке(СтрокаПроверки, ...)`

**Преобразования:**

- `СократитьДвойныеКавычки(Значение)`
- `УдалитьПоследнийСимволВСтроке(Текст, ЧислоСимволов = 1)` — изменяет аргумент по ссылке
- `СформироватьСтрокуСимволов(Символ, ДлинаСтроки)`
- `ДополнитьСтроку(Значение, ДлинаСтроки, Символ = "0", Режим = "Слева")`
- `УдалитьПовторяющиесяСимволы(Значение, УдаляемыйСимвол, Режим = "Слева")`
- `ЗаменитьОдниСимволыДругими(ЗаменяемыеСимволы, Значение, СимволыЗамены)`
- `ПреобразоватьЧислоВРимскуюНотацию(АрабскоеЧисло, ИспользоватьКириллицу = Истина)`
- `ПреобразоватьЧислоВАрабскуюНотацию(РимскоеЧисло, ИспользоватьКириллицу = Истина)`
- `ИзвлечьТекстИзHTML(ИсходныйТекст)`
- `СтрокаВЧисло(Значение)`, `СтрокаВДату(Значение, ЧастьДаты = Неопределено)` — без исключений на парсинге
- `СтрокаСЧисломДляЛюбогоЯзыка(Шаблон, Число, Вид = Неопределено, ФорматнаяСтрока = Неопределено)` — словоформы

### СтроковыеФункции / СтроковыеФункцииКлиент

- `ФорматированнаяСтрока(ШаблонСтроки, Параметр1 = Неопределено, ...)` — возвращает `ФорматированнаяСтрока` с разметкой типа `<b>...</b>`. Есть на сервере и на клиенте.
- `СтрокаЛатиницей(Значение)` — транслитерация. Есть на сервере и на клиенте.
- `СтроковыеФункции.ПредставлениеПериодаВТексте(ДатаНачала, ДатаОкончания, ФорматнаяСтрока = "", СЗаглавнойБуквы = Ложь)` — только на сервере.

---

## Пользователи / *Клиент / *КлиентСервер

### Текущий контекст

Серверные `Пользователи` и клиентские `ПользователиКлиент`:

- `АвторизованныйПользователь()` — текущий пользователь ИБ как ссылка справочника
- `ТекущийПользователь()` — то же, но с учётом сеанса от имени другого пользователя (`Пользователи` БСП)
- `ЭтоСеансВнешнегоПользователя()`

### Проверки прав

Серверный `Пользователи`:

- `ЭтоПолноправныйПользователь(Пользователь = Неопределено, ...)` — проверка ролей администрирования
- `РолиДоступны(ИменаРолей, ...)`
- `ВходВПрограммуРазрешен(ОписаниеПользователяИБ)`

### Поиск пользователей

- `НайтиПоИмени(ИмяДляВхода)`
- `НайтиНеоднозначныхПользователейИБ(Пользователь, ...)`
- `ПользовательИБЗанят(ПользовательИБ, УчетнаяЗапись = Неопределено)`

### Неуказанный пользователь

- `ПолноеИмяНеуказанногоПользователя()`
- `СсылкаНеуказанногоПользователя(СоздатьЕслиНеСуществует = Ложь)`

### Управление учётными записями

- `АвторизоватьТекущегоПользователяПриВходе(ВызыватьИсключение = Истина)`
- `СоздатьАдминистратора(ПользовательИБ = Неопределено)`
- `НовоеОписаниеПользователяИБ()`
- `СвойстваПользователяИБ(ИмяИлиИдентификатор)`
- `УстановитьСвойстваПользователяИБ(ИмяИлиИдентификатор, ОбновляемыеСвойства, ...)`
- `УдалитьПользователяИБ(ИмяИлиИдентификатор)`
- `СкопироватьСвойстваПользователяИБ(Приемник, ...)`
- `ПриНаличииГруппПользователейУстановитьИспользование()`

### Пароли

- `СохраняемоеЗначениеСтрокиПароля(Пароль)` — хеш для сохранения
- `СоздатьПароль(НаименьшаяДлина = 7, Сложный = Ложь, УчестьНастройки = "ДляПользователей")`

### Назначение ролей

- `НазначениеРолей()` — описание раздач ролей по типу пользователя
- `ПроверитьНазначениеРолей(ПроверитьВсе = Ложь, СписокОшибок = Неопределено)`
- `УстановитьПравоОткрытияВнешнихОтчетовИОбработок(ОткрытиеРазрешено)` — важно при работе с внешними отчётами и обработками

### UI

- `СформироватьДанныеВыбораПользователя(Текст, ...)` — для полей с выбором пользователей
- `ЗаполнитьНомераКартинокПользователей(ТаблицаИлиДерево, ...)`

### Прочее

- `ОбщиеНастройкиВходаИспользуются()`

### ПользователиКлиентСервер

Содержит только `УстаревшиеПроцедурыИФункции` — публичного API нет, **не использовать**.

---

## ФайловаяСистема (сервер) / ФайловаяСистемаКлиент

### Временные файлы (серверный `ФайловаяСистема`)

- `СоздатьВременныйКаталог(Расширение = "")` — возвращает путь
- `УдалитьВременныйКаталог(Путь)`
- `УдалитьВременныйФайл(Путь)`
- `УникальноеИмяФайла(ИмяФайла)`

### Запуск программ

- `ФайловаяСистема.ПараметрыЗапускаПрограммы()` / `ФайловаяСистема.ЗапуститьПрограмму(КомандаЗапуска, ПараметрыЗапускаПрограммы = Неопределено)` — серверный запуск
- `ФайловаяСистемаКлиент.ПараметрыЗапускаПрограммы()` / `ФайловаяСистемаКлиент.ЗапуститьПрограмму(КомандаЗапуска, ПараметрыЗапускаПрограммы = Неопределено)` — клиентский, асинхронный

> Брать вместо платформенных `ЗапуститьПриложение`/`НачатьЗапускПриложения` всегда — БСП-обёртки кросс-платформенные и обрабатывают веб-клиента.

### Открытие в проводнике/браузере (Клиент)

- `ФайловаяСистемаКлиент.ОткрытьФайл(...)`
- `ФайловаяСистемаКлиент.ОткрытьПроводник(ПутьККаталогуИлиФайлу)`
- `ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(НавигационнаяСсылка, Оповещение = Неопределено)`

### Загрузка/сохранение файлов с диалогом (Клиент)

- `ЗагрузитьФайл(...)` / `ЗагрузитьФайлы(...)` — диалог открытия с загрузкой во временное хранилище
- `СохранитьФайл(ОбработчикЗавершения, АдресВоВременномХранилище, ИмяФайла = "", ...)`
- `СохранитьФайлы(ОбработчикЗавершения, СохраняемыеФайлы, ПараметрыСохранения = Неопределено)`

**Структуры параметров:**

- `ПараметрыЗагрузкиФайла()`
- `ПараметрыСохраненияФайла()` / `ПараметрыСохраненияФайлов()`
- `ПараметрыОткрытияФайла()`

### Прочее (Клиент)

- `ВыбратьКаталог(ОбработчикЗавершения, Заголовок = "", Каталог = "")`
- `ПоказатьДиалогВыбора(ОбработчикЗавершения, Диалог)`
- `СоздатьВременныйКаталог(Оповещение, Расширение = "")` — клиентский, асинхронный
- `ПодключитьРасширениеДляРаботыСФайлами(...)` — расширение работы с файлами для веб-клиента
- `УникальноеИмяФайла(ИмяФайла)`

---

## Антипаттерны

| Не делать | Использовать |
| --- | --- |
| `Сообщить("текст")` | `ОбщегоНазначения.СообщитьПользователю(...)` / `ОбщегоНазначенияКлиент.СообщитьПользователю(...)` |
| `Ссылка.Реквизит` | `ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, "Реквизит")` |
| `Ссылка.Реквизит1, Ссылка.Реквизит2` (несколько обращений) | `ЗначенияРеквизитовОбъекта(Ссылка, "Реквизит1,Реквизит2")` |
| `Структура.Свойство("Ключ", Знач)` для дефолта | `ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Структура, "Ключ", ЗначениеПоУмолчанию)` |
| Ручное добавление массивов в цикле | `ОбщегоНазначенияКлиентСервер.ДополнитьМассив` / `СвернутьМассив` |
| `ХранилищеОбщихНастроек.Сохранить(...)` напрямую | `ОбщегоНазначения.ХранилищеОбщихНастроекСохранить(...)` (работает в модели сервиса) |
| `ТекущаяДата()` | `ТекущаяДатаСеанса()` (платформа) на сервере, `ОбщегоНазначенияКлиент.ДатаСеанса()` на клиенте |
| Платформенный `НачатьЗапускПриложения` | `ФайловаяСистемаКлиент.ЗапуститьПрограмму(...)` (или серверный аналог) |
| `Найти(ИмяФайла, "/")` для разбора пути | `ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(...)` |
| Склейка путей через `+ "/" +` | `ОбщегоНазначенияКлиентСервер.ПолучитьПолноеИмяФайла(Каталог, ИмяФайла)` |
| `ПредопределенноеЗначение("Справочник.Х.Имя")` | `ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.Х.Имя")` |
| `Выполнить(Код)` для внешнего кода | `ОбщегоНазначения.ВыполнитьВБезопасномРежиме(Алгоритм, ...)` |
| `СтрРазделить(Стр, ",")` если нужны опции пропуска пустых | `СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(...)` |
| Конкатенация шаблонов через `+` | `СтрШаблон` (платформа) или `ПодставитьПараметрыВСтроку(...)` |
| Жёстко зашитые цвета/шрифты на клиенте | `ОбщегоНазначенияКлиент.ЦветСтиля(...)` / `ШрифтСтиля(...)` |
| Ручная проверка ролей через `РольДоступна` для админа | `Пользователи.ЭтоПолноправныйПользователь()` |

## Что использовать запрещено

- Методы из `#Область УстаревшиеПроцедурыИФункции` — БСП помечает их к удалению.
- Методы из `#Область СлужебныйПрограммныйИнтерфейс` — для подсистем БСП, не для расширений.
- Модули с суффиксами `*Служебный`, `*ПовтИсп`, `*Переопределяемый` — внутренняя кухня БСП.
- Модули с суффиксом `*БТС` — это библиотека технологии сервиса (БТС), отдельная подсистема, не базовый набор БСП.

## Где смотреть полные описания

- Подробные doc-комментарии — в исходниках БСП хост-проекта базовой конфигурации. Путь к модулю: `<хост-проект>/src/CommonModules/<ИмяМодуля>/Module.bsl` (каталог — см. [`../CLAUDE.md`](../CLAUDE.md) → «EDT-воркспейс»)
- Платформенные методы и типы — через `/bsl-check` (skill `bsl-check`)
- Сценарии работы с инструментами codepilot1c — [`docs/codepilot1c-reference.md`](codepilot1c-reference.md)
