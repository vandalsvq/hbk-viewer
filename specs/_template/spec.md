---
issue: <prefix>-<N>
title: <краткое название фичи>
status: draft  # draft | approved | in-progress | done | superseded
owner: <имя>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
subsystems:
  - <подсистема_1 | подсистема_2 | ... >
---

# <prefix>-<N>: <краткое название>

> Ссылка на issue: `<URL>`

Этот файл — стабильный контракт фичи: что должно быть реализовано и как
пользователь/вызывающий код это видит. Переживает merge и уезжает в
`specs/done/<prefix>-<N>/spec.md`.

Транзиентные артефакты (этапы работ, открытые вопросы, история решений) —
в соседнем `plan.md`.

## 1. Контекст и проблема

Зачем это нужно. Какую задачу пользователя/разработчика решаем. Что сейчас
не работает или работает плохо. Ссылки на связанные issues, обсуждения,
предыдущие спеки.

## 2. Область изменений

Что затрагивается. Укажи конкретные обработки, общие модули, метаданные,
формы, роли. Важно для соблюдения архитектурных ограничений из CLAUDE.md.

| Компонент | Тип | Характер изменений |
| --- | --- | --- |
| `<имя>` | обработка / общий модуль / форма | добавить / изменить / удалить |

**Проверка архитектурных ограничений:**

- [ ] Нет новых зависимостей у изолированных подсистем (ядро, клиентские модули)
- [ ] Не нарушены ограничения, описанные в CLAUDE.md → раздел «Ключевые обработки»

## 3. Поведение

Что должно происходить. Пошагово, с точки зрения пользователя или
вызывающего кода. Граничные случаи. Состояния ошибок.

### 3.1. Основной сценарий

1. ...
2. ...

### 3.2. Граничные случаи

- ...

### 3.3. Ошибки

- ...

## 4. Контракты

Сигнатуры новых или изменяемых экспортных методов, структуры-конструкторы,
реквизиты объектной модели, форматы XDTO.

> **Язык имён:** имена методов, переменных, реквизитов — на русском.
> **Исключение:** типы и свойства XDTO-пакетов / публичных API
> сериализации — в оригинальном английском написании, так как это
> публичный контракт.

### 4.1. Экспортные методы

```bsl
// <Описание — начинается с глагола. Для функций: «Возвращает…».>
//
// Параметры:
//  Параметр1 - Тип - описание.
//
// Возвращаемое значение:
//  Тип - описание.
//
Функция ИмяФункции(знач Параметр1) Экспорт
```

### 4.2. Структуры-конструкторы (std641)

```bsl
// Возвращаемое значение:
//  Структура:
//    * Поле1 - Тип
//    * Поле2 - Тип
Функция НоваяНастройка() Экспорт
```

### 4.3. Реквизиты объектной модели / XDTO

| Имя | Тип | Описание |
| --- | --- | --- |
| `<Object>.<Prop>` | String | ... |

## 5. Критерии приёмки

Проверяемый чек-лист. Каждый пункт — то, что можно ручно проверить в UI
или через консоль команд. Когда появится Vanessa/YAxUnit — мигрирует в
`.feature` / тесты.

- [ ] ...
- [ ] ...

## 6. Вне области

Явный перечень того, что **НЕ** делается в этой спеке. Защита от
расползания объёма.

- ...
