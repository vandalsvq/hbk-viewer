# CLAUDE.md

Руководство для работы Claude Code над проектом 1С:Предприятие в EDT.

> Это **шаблон**. Перед началом работы заполните разделы со знаком `<...>` под свой проект и удалите этот блок-предупреждение.

## Проект

**`<НазваниеПроекта>`** — `<краткое описание: расширение / конфигурация / библиотека>` для 1С:Предприятие.

- EDT-проект: `<Каталог.Имя>/`
- Префикс всех объектов: `<пример: prj_>`
- Платформа: `<пример: 1C:Enterprise 8.3.21+>`
- Трекер задач: `<URL issues или система>`

## EDT-воркспейс

Если проект состоит из нескольких EDT-проектов (например, расширение + базовая конфигурация-хост), опишите их здесь:

| Каталог | Проект | Роль |
| --- | --- | --- |
| `<путь>` | `<имя>` | базовая конфигурация / расширение / библиотека |

Когда нужно найти модуль БСП (`ОбщегоНазначения`, `РаботаСФайлами`, `УправлениеСвойствами` и т.п.) или понять поведение хоста — искать в каталоге базовой конфигурации.

## Инструменты

В проекте может быть подключён MCP-сервер **codepilot1c**. Полный справочник инструментов со сценариями: [`docs/codepilot1c-reference.md`](docs/codepilot1c-reference.md).

**Правила выбора:**

- **Поиск по паттерну** (методы, вызовы, вхождения) → `Grep` / `Glob` — в первую очередь, экономнее MCP на порядок
- **Типизация, анализ логики, метаданные, формы, СКД** → инструменты `codepilot1c`
- **Чтение для редактирования** → `Read` (обязательно перед `Edit`)
- **Сомнения в API встроенного типа** → skill `bsl-check` (обёртка над `шcntx_ru`)
- **После изменений** → `edt_diagnostics`

**Обязательно перед любой мутацией** (метаданные, форма, СКД, расширение, внешний объект): вызвать `edt_validate_request` → получить `validation_token` → передать в мутирующий инструмент. Перед созданием нового модуля — `ensure_module_artifact`.

Большинство MCP-инструментов **deferred** — раскрываются через `discover_tools(category=...)`.

### Skill `bsl-check`

```text
/bsl-check Структура.Вставить        ✓ Найти метод с сигнатурой
/bsl-check -m Массив                 ✓ Все методы типа
/bsl-check -p Строка                 ✓ Все свойства типа
/bsl-check -s Удалить                ✓ Поиск по названию
/bsl-check Структура.Вставить --full ✓ Полный вывод со ссылками
```

Скрипт работает поверх локального справочника синтаксиса 1С из публичного репозитория [`vandalsvq/hbk_md`](https://github.com/vandalsvq/hbk_md). Чеклист первичной настройки — [`docs/bsl-check-setup.md`](docs/bsl-check-setup.md).

### Skill `sync-template`

`/sync-template [пути]` — подтянуть переносимые файлы (docs со стандартами, скиллы, шаблоны спек) из репозитория-шаблона [`edt1c-ai-template`](https://github.com/vandalsvq/edt1c-ai-template) в проект: сверка diff по каждому файлу, автозамена нейтрального префикса `prj_` на префикс проекта из CLAUDE.md. В самом репозитории шаблона не применяется.

## Соглашения по коду

- Язык кода и комментариев — **русский**
- Имена переменных, процедур, реквизитов — на русском
- Клиент-серверные модули именуются `*КлиентСервер`
- Строгая типизация (`// @strict-types`) включается во всех новых модулях
- Все новые объекты — с префиксом проекта (`<prefix>_`)

## Стандарты кода BSL

Базовые правила — ниже. Подробные руководства с примерами:

| Тема | Документ |
| --- | --- |
| Общие стандарты (именование, структура модулей, форматирование) | [`docs/bsl-coding-standards.md`](docs/bsl-coding-standards.md) |
| Антипаттерны (полный список с примерами) | [`docs/bsl-anti-patterns.md`](docs/bsl-anti-patterns.md) |
| Оптимизация запросов | [`docs/bsl-query-optimization.md`](docs/bsl-query-optimization.md) |
| Справочник синтаксиса запросов | [`docs/bsl-query-reference.md`](docs/bsl-query-reference.md) |
| Функции языка запросов | [`docs/bsl-query-functions.md`](docs/bsl-query-functions.md) |
| Правила модулей форм | [`docs/bsl-form-module-rules.md`](docs/bsl-form-module-rules.md) |
| Зарезервированные имена форм | [`docs/bsl-form-reserved-names.md`](docs/bsl-form-reserved-names.md) |
| Асинхронные методы (Асинх/Ждать/Обещание) | [`docs/bsl-async.md`](docs/bsl-async.md) |
| Строгая типизация `// @strict-types` | [`docs/bsl-strict-types.md`](docs/bsl-strict-types.md) |
| Целостность MDO-файлов | [`docs/mdo-integrity.md`](docs/mdo-integrity.md) |
| Рефакторинг | [`docs/bsl-refactoring.md`](docs/bsl-refactoring.md) |
| Ревью кода | [`docs/bsl-code-review.md`](docs/bsl-code-review.md) |
| Общие модули БСП (`ОбщегоНазначения`, `СтроковыеФункции`, `Пользователи`, `ФайловаяСистема` + клиент/клиент-серверные варианты) | [`docs/bsp-common-modules.md`](docs/bsp-common-modules.md) |
| Выбор модели Claude | [`docs/model-selection.md`](docs/model-selection.md) |

### Ключевое, что нужно держать в голове

**Структура общего модуля** — строго в таком порядке:

1. `#Область ПрограммныйИнтерфейс` — экспортные, документированы
2. `#Область СлужебныйПрограммныйИнтерфейс` — экспортные для подсистемы
3. `#Область СлужебныеПроцедурыИФункции` — неэкспортные

**Запросы — только русскоязычный синтаксис** (`ВЫБРАТЬ`, `ЛЕВОЕ СОЕДИНЕНИЕ`, `СГРУППИРОВАТЬ ПО`), всегда псевдонимы через `КАК`, псевдоним таблицы ≠ имя поля, `ТекущаяДатаСеанса()` вместо `ТекущаяДата()`.

**Критические антипаттерны** (CRITICAL):

- Запрос в цикле
- Обращение к реквизитам через точку (`Ссылка.Реквизит`)
- Подзапрос в SELECT
- Фильтр виртуальной таблицы в `ГДЕ` вместо параметров ВТ

**Транзакции** — `НачатьТранзакцию()` строго **до** `Попытка`:

```bsl
НачатьТранзакцию();
Попытка
    // ...
    ЗафиксироватьТранзакцию();
Исключение
    ОтменитьТранзакцию();
    ВызватьИсключение;
КонецПопытки;
```

**Сообщения и исключения:**

- Не `Сообщить()` → `ОбщегоНазначения.СообщитьПользователю()` / `ОбщегоНазначенияКлиент.СообщитьПользователю()`
- Исключения не глотать: логировать через `ЗаписьЖурналаРегистрации` и пробрасывать `ВызватьИсключение`

**Пользовательские тексты** — только через `НСтр("ru = 'Текст'")`, не голым строковым литералом (сообщения, предупреждения, заголовки, тексты вопросов). Подстановки — `СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку`. Подробнее — [`docs/bsl-coding-standards.md`](docs/bsl-coding-standards.md).

**Модули форм** ([`docs/bsl-form-module-rules.md`](docs/bsl-form-module-rules.md)):

- Реквизиты и свойства формы — через `ЭтотОбъект.` (исключения только `Объект` и `Элементы`); `ЭтаФорма` устарел
- Серверные методы (`&НаСервере`, `&НаСервереБезКонтекста`) обязаны оканчиваться на `НаСервере`
- Метод не использует контекст формы → `&НаСервереБезКонтекста`; чисто вычислительный над данными → `&НаКлиентеНаСервереБезКонтекста` с параметром `Форма`/`Объект`
- Не вызывать сервер в циклах
- Предупреждение без ожидания ответа — `ПоказатьПредупреждение` без обработчика, не `ПредупреждениеАсинх` ([`docs/bsl-async.md`](docs/bsl-async.md))

**Строгая типизация** ([`docs/bsl-strict-types.md`](docs/bsl-strict-types.md)): `// @strict-types` — первая строка новых модулей; `знач` (нижний регистр) для неизменяемых параметров; `Перем` без начального значения запрещён; смена типа переменной запрещена. Doc-блок с типами (`Параметры` / `Возвращаемое значение`) обязателен для **всех** методов, включая неэкспортные и обработчики событий.

**Форматирование:** однотипные блоки подряд (`=`, `Структура.Вставить`, инлайн-типы) выравнивать обязательно; добор до `=` — табами до общей табопозиции (мелкий добор 1–2 пробела — исключение); отступы — табами.

## Ветки и коммиты

- Релизная ветка: `<пример: master / main>`
- Основная ветка разработки: `<пример: develop>`
- Рабочие ветки: `<пример: feature/<prefix>-<N>>` от ветки разработки
- Формат коммита: `<URL issue> <описание на русском>`

**Версия конфигурации** — повышайте перед коммитом, затрагивающим каталог EDT-проекта. Файл: `<EDT-проект>/src/Configuration/Configuration.mdo`, атрибут `version`.

Соглашение об инкременте версии (пример):

- **Промежуточный коммит внутри задачи** — четвёртый сегмент +1 (`2026.1.4.0` → `2026.1.4.1`)
- **Коммит, закрывающий задачу** — третий сегмент +1, четвёртый в `0` (`2026.1.4.7` → `2026.1.5.0`)
- Коммит только с файлами вне EDT-проекта (планы, спеки, docs) — версию не менять

## Структура

```text
<EDT-проект>/
└── src/
    ├── CommonModules/         # Общие модули
    ├── DataProcessors/        # Обработки
    ├── CommonForms/           # Общие формы
    ├── CommonTemplates/       # Общие макеты
    ├── Catalogs/              # Справочники
    ├── InformationRegisters/  # Регистры сведений
    ├── Roles/                 # Роли доступа
    └── Subsystems/            # Подсистемы
```

### Ключевые подсистемы

| Подсистема | Назначение |
| --- | --- |
| `<подсистема_1>` | `<описание>` |

### Ключевые обработки и их зависимости

| Обработка | Назначение | Допустимые зависимости |
| --- | --- | --- |
| `<обработка>` | `<описание>` | `<список>` |

> **Архитектурные ограничения (СТРОГО):**
>
> Опишите здесь правила зависимостей: какие модули/обработки изолированы, какие могут зависеть только от ограниченного набора. Нарушение → циклические зависимости и проблемы при сборке.

### Ключевые общие модули

| Модуль | Назначение |
| --- | --- |
| `<модуль>` | `<описание>` |

### Роли

`<перечислить роли доступа проекта>`.

## Тестирование

- **YAxUnit** (unit/integration) — `author_yaxunit_tests` создаёт/обновляет общий модуль с тестами, синхронизирует ИсполняемыеСценарии. Применять только после интеграции фреймворка
- **Vanessa Automation** (BDD/E2E) — `qa_inspect` → `qa_prepare_form_context` → `qa_plan_scenario` → `qa_generate` → `qa_validate_feature` → `qa_run`. Применять только после интеграции

## Specification-Driven Development

Проект ведётся по методологии **SDD: значимое изменение начинается со спецификации**, а не с кода. Спека живёт в [`specs/`](specs/) и является источником истины для поведения.

- Процесс, именование, статусы: [`specs/README.md`](specs/README.md)
- Шаблоны: [`specs/_template/spec.md`](specs/_template/spec.md) (стабильный контракт) + [`specs/_template/plan.md`](specs/_template/plan.md) (этапы, решения — транзиентное)
- Ретроактивные спеки: [`specs/retro/`](specs/retro/)
- Черновые планы до спек (груминг бэклога, зонтичные инициативы, реестры): [`planning/`](planning/) — когда писать туда, а когда сразу в спеку, см. [`planning/README.md`](planning/README.md)

Активная спека — папка `specs/<prefix>-<N>/` с `spec.md` + `plan.md`. Порог выделения `decisions.md` из `plan.md` — ~15+ записей в разделе «Решения».

**Когда спека обязательна:**

- Новая фича или изменение поведения, видимого пользователю
- Изменение публичного контракта: экспортные методы общих модулей, реквизиты ключевых обработок, XDTO-пакеты
- Изменение затрагивает несколько обработок/подсистем

**Когда спека не нужна:** опечатка, локальный багфикс без изменения контракта, рефакторинг без изменения поведения, обновление зависимостей и конфигов.

**Язык спек** — русский. **Исключение:** имена объектов публичного контракта сериализации (XDTO, типы интеграций) — на оригинальном языке (обычно английском).

## Цикл работы над фичей

1. **Спека первой.** Найти или создать `specs/<prefix>-<N>/` (`spec.md` + `plan.md` по шаблонам). Согласовать → статус `approved`. Без этого к коду не приступать
2. Работа в ветке `feature/<prefix>-<N>` от ветки разработки
3. Перед изменениями — `Grep` для поиска, затем `Read` / `bsl_get_method_body` для контекста
4. Не нарушать архитектурные ограничения зависимостей
5. Перед мутацией метаданных/формы — `edt_validate_request` → токен
6. Перед созданием нового модуля — `ensure_module_artifact`
7. После изменений — `edt_diagnostics`
8. Для нетривиальной логики — YAxUnit через `author_yaxunit_tests` (когда подключится фреймворк)
9. **Расхождение кода и `spec.md` — обновляется спека тем же PR.** Транзиентное (этапы, решения) — в `plan.md`
10. Коммитить атомарно с привязкой к issue и ссылкой на спеку
11. После merge и закрытия issue: статус `spec.md` → `done`, папка переносится в `specs/done/<prefix>-<N>/`. Ретро-спеки и `superseded` не переносятся
